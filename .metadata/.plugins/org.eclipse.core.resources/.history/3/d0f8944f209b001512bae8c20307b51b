package day3;

public class Calculator {
	int getArraySum(int[] subject) {
		int size = subject.length;
		int sum = 0;
		for (int i = 0; i < size; i++) {
			sum = sum + subject[i];
		}
		return (subject[0] + subject[1] + subject[2]);
	}
}
